[project]
name = "dehead"
version = "0.1.0"
description = "dehead is a simple command-line tool for automatic anonymization"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ultralytics>=8.4.40",
]
